# Env

환경 변수 리스트

```yaml
PORT = 서버 실행 포트

# database

DB_HOST = 
DB_ID = DB 계정 이름
DB_NAME = DB 이름
DB_PW = DB 비밀번호

# jsonwebtoken

JWT_SECRET = 암호화 토큰 값
JWT_ALGORITHM = 암호화 방식

# bcrpyt

BCRYPT_SALT = 암호화 횟수
```